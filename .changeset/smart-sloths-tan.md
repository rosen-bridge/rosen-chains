---
'@rosen-chains/abstract-chain': minor
---

Add `getTransactionRosenData` to `AbstractChain` which calls rosen-extractor function (this enables overriding the process of getting RosenData in child chains)
